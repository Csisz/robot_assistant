#pragma once

/* Legacy single-network shape.
 * New firmware prefers wifi_secrets.h for multiple WiFi profiles.
 * Both real files are git-ignored and should never be committed. */

#define WIFI_SSID "your_network_name"
#define WIFI_PASS "your_password"
